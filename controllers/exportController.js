import XLSX from "xlsx"; import Schedule from "../models/Schedule.js"; export async function exportSchedulesToExcel(req, res) { const schedules = await Schedule.find().populate("employee").sort({ date: 1 }); const rows = schedules.map((item) => ({ EmployeeID: item.employee?.employeeId || "", EmployeeName: item.employee?.name || "", Department: item.employee?.department || "", Date: item.date, Shift: item.shift, StartTime: item.startTime, EndTime: item.endTime, Location: item.location, Status: item.status, Completed: item.completed ? "Yes" : "No", })); const workbook = XLSX.utils.book_new(); const worksheet = XLSX.utils.json_to_sheet(rows); XLSX.utils.book_append_sheet(workbook, worksheet, "Schedules");
const buffer = XLSX.write(workbook, { type: "buffer", bookType: "xlsx" }); res.setHeader( "Content-Disposition", "attachment; filename=workforce-schedules.xlsx" ); res.setHeader( "Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" ); res.send(buffer); }