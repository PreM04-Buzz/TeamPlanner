import mongoose from "mongoose"; const scheduleSchema = new mongoose.Schema( { employee: { type: mongoose.Schema.Types.ObjectId, ref: "Employee", required: true, },
date: { type: String, required: true, }, shift: { type: String, enum: ["Morning", "Afternoon", "Evening", "Night"], required: true, }, startTime: { type: String, default: "09:00", }, endTime: { type: String, default: "17:00", }, location: { type: String, default: "", }, status: { type: String, enum: ["Pending Supervisor Approval", "Approved", "Rejected", "Completed"], default: "Pending Supervisor Approval", }, completed: { type: Boolean, default: false, }, }, { timestamps: true } ); export default mongoose.model("Schedule", scheduleSchema);